#!/usr/bin/env python3

import os

files = os.listdir(".")

i = 0
while i < len(files):
    file = files[i].split(".")
    if file[-1] != ".bak":
        backup = file[0] + "." + file[-1] + ".bak"
        with open(files[i]) as f:
            t = f.readline()
            j = 0
            while j < len()
        i += 1
